# Prompt the user for their name
name = input("Please enter your name: ")

# Greet the user
print(f"Hello, {name}!")

# Calculate and display the length of their name
name_length = len(name)
print(f"Your name has {name_length} characters.")